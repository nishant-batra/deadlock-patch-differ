# Google AdSense publisher/client ID (ca-pub-XXXXXXXXXXXXXXXX).
# Not a secret - it's already public in every page's rendered HTML once ads
# are live. Left unset in dev/preview; the ad script and slots simply don't
# render until it's configured.
VITE_ADSENSE_PUBLISHER_ID=
